describe("lerna-import", () => {
  it("should do nothing if sub-tree exists and is current", () => {})

  it("should import a new sub-tree package", () => {
    //
  })

  it("should pull changes into a new sub-tree package", () => {
    // ensure that tags are imported if necessary.
  })
})
